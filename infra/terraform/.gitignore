# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Do not commit real variable values / secrets
terraform.tfvars
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Keep: .terraform.lock.hcl (provider version pins), *.tfvars.example
!*.tfvars.example
