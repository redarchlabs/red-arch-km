"""The multi-provider agent org: agents, work orders, delegation, runtime.

This package is the durable, provider-agnostic successor to the single in-API
config assistant (``api.services.agent``). The older module is intentionally left
intact; new code lives here under the plural ``agents`` package.
"""
