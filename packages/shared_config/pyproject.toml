[project]
name = "shared-config"
version = "1.0.0"
description = "Shared Pydantic Settings for all Red Arch KM services"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared_config"]
